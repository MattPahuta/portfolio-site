---
import { Icon } from "astro-icon/components";
const { name, icon } = Astro.props;
---

<Icon
  name={`${icon}`}
  title={`${name}`}
  class="size-8 text-indigo-500"
  aria-hidden="true"
  focusable="false"
/>