---
import { Icon } from "astro-icon/components";
const { name, icon } = Astro.props;
---

<Icon
  name={`${icon}`}
  title={`${name}`}
  class="size-8 md:size-10 text-emerald-200/80"
  aria-hidden="true"
  focusable="false"
/>